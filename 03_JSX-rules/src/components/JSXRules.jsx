import React from 'react'

const JSXRules = () => {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>JSX must return a <b>single parent element.</b></p>
      <p>JSX elements must be <b>properly closed</b></p>
      <p>JSX attributes are written using <b>camelCase</b> (e.g. className instead of class)</p>
    </div>
  )
}

export default JSXRules
